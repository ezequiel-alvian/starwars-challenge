Se instalan los paquetes de node.

npm i 

luego se crea un archivo .env donde se agrega la siguiente url.

REACT_APP_API_STAR_WARS=https://swapi.dev/api/people

se corre la app con el comando

npm run start